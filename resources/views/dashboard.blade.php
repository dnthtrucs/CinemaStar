@extends('layouts.app')
@section('title', 'Tài khoản')
@section('content')
<div class="container py-5"><h1 class="section-title">Xin chào, {{ auth()->user()->name }}</h1><p class="text-muted">Theo dõi vé và những suất chiếu sắp tới của bạn.</p><div class="row g-3 my-4"><div class="col-md-4"><div class="card p-4 shadow-soft"><div class="text-muted">Tổng đơn</div><div class="display-6 fw-bold">{{ $stats['total'] }}</div></div></div><div class="col-md-4"><div class="card p-4 shadow-soft"><div class="text-muted">Vé sắp xem</div><div class="display-6 fw-bold text-success">{{ $stats['upcoming'] }}</div></div></div><div class="col-md-4"><div class="card p-4 shadow-soft"><div class="text-muted">Chờ thanh toán</div><div class="display-6 fw-bold text-warning">{{ $stats['pending'] }}</div></div></div></div><div class="d-flex justify-content-between align-items-center"><h3 class="fw-bold">Đơn gần đây</h3><a href="{{ route('bookings.index') }}">Xem tất cả</a></div><div class="card shadow-soft mt-3"><div class="list-group list-group-flush">@forelse($bookings as $booking)<a href="{{ route('bookings.show',$booking) }}" class="list-group-item list-group-item-action p-3"><div class="d-flex justify-content-between"><div><strong>{{ $booking->showtime->movie->title }}</strong><div class="small text-muted">{{ $booking->showtime->starts_at->format('H:i d/m/Y') }} · {{ $booking->tickets->pluck('seat.label')->join(', ') }}</div></div><span class="fw-bold">{{ number_format($booking->total_price,0,',','.') }}₫</span></div></a>@empty<div class="p-4 text-muted">Chưa có đơn đặt vé.</div>@endforelse</div></div></div>
@endsection
