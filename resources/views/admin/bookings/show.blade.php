@extends('layouts.app')
@section('title','Đơn '.$booking->code)
@section('content')<div class="container py-4">@include('admin.partials.nav')<h1 class="section-title">Chi tiết đơn {{ $booking->code }}</h1><div class="row g-4 mt-1"><div class="col-lg-7"><div class="card shadow-soft p-4"><h5 class="fw-bold">Thông tin vé</h5><dl class="row mb-0"><dt class="col-sm-4">Khách hàng</dt><dd class="col-sm-8">{{ $booking->user->name }} · {{ $booking->user->email }}</dd><dt class="col-sm-4">Phim</dt><dd class="col-sm-8">{{ $booking->showtime->movie->title }}</dd><dt class="col-sm-4">Suất chiếu</dt><dd class="col-sm-8">{{ $booking->showtime->starts_at->format('H:i d/m/Y') }}</dd><dt class="col-sm-4">Rạp / phòng</dt><dd class="col-sm-8">{{ $booking->showtime->room->cinema->name }} / {{ $booking->showtime->room->name }}</dd><dt class="col-sm-4">Ghế</dt><dd class="col-sm-8">{{ $booking->tickets->pluck('seat.label')->join(', ') }}</dd><dt class="col-sm-4">Tổng tiền</dt><dd class="col-sm-8 fw-bold text-brand">{{ number_format($booking->total_price,0,',','.') }}₫</dd></dl></div></div><div class="col-lg-5"><div class="card shadow-soft p-4"><h5 class="fw-bold">Lịch sử thanh toán</h5>@forelse($booking->payments as $payment)<div class="border-bottom py-3"><div class="d-flex justify-content-between"><strong>{{ strtoupper($payment->provider) }}</strong><span class="badge text-bg-{{ $payment->status==='success'?'success':'secondary' }}">{{ $payment->status }}</span></div><div class="small text-muted">{{ $payment->request_id }} · {{ $payment->created_at->format('H:i d/m/Y') }}</div></div>@empty<div class="text-muted">Chưa có giao dịch.</div>@endforelse</div></div></div></div>@endsection
