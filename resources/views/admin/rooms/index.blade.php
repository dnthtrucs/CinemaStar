@extends('layouts.app')
@section('title','Quản lý phòng')
@section('content')<div class="container py-4">@include('admin.partials.nav')<div class="d-flex justify-content-between mb-3"><h1 class="section-title">Phòng chiếu & sơ đồ ghế</h1><a href="{{ route('admin.rooms.create') }}" class="btn btn-primary">Thêm phòng</a></div><div class="card shadow-soft p-3"><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Rạp</th><th>Phòng</th><th>Loại</th><th>Sơ đồ</th><th>Trạng thái</th><th></th></tr></thead><tbody>@foreach($rooms as $room)<tr><td>{{ $room->cinema->name }}</td><td class="fw-bold">{{ $room->name }}</td><td>{{ $room->type }}</td><td>{{ $room->rows }} hàng × {{ $room->seats_per_row }} ghế ({{ $room->seats_count }})</td><td><span class="badge text-bg-{{ $room->is_active?'success':'secondary' }}">{{ $room->is_active?'Hoạt động':'Tạm dừng' }}</span></td><td class="text-end"><a class="btn btn-outline-dark btn-sm" href="{{ route('admin.rooms.edit',$room) }}"><i class="bi bi-pencil"></i></a><form class="d-inline" method="POST" action="{{ route('admin.rooms.destroy',$room) }}">@csrf @method('DELETE')<button class="btn btn-outline-danger btn-sm"><i class="bi bi-trash"></i></button></form></td></tr>@endforeach</tbody></table></div>{{ $rooms->links() }}</div></div>@endsection
