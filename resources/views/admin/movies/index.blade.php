@extends('layouts.app')
@section('title','Quản lý phim')
@section('content')<div class="container py-4">@include('admin.partials.nav')<div class="d-flex justify-content-between align-items-center mb-3"><h1 class="section-title">Quản lý phim</h1><a href="{{ route('admin.movies.create') }}" class="btn btn-primary"><i class="bi bi-plus-lg me-1"></i>Thêm phim</a></div><div class="card shadow-soft p-3"><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Phim</th><th>Thể loại</th><th>Khởi chiếu</th><th>Trạng thái</th><th class="text-end">Hành động</th></tr></thead><tbody>@foreach($movies as $movie)<tr><td><div class="d-flex align-items-center gap-2"><img src="{{ $movie->poster }}" class="rounded" style="width:42px;height:60px;object-fit:cover"><div><strong>{{ $movie->title }}</strong><div class="small text-muted">{{ $movie->duration }} phút · {{ $movie->age_rating }}</div></div></div></td><td>{{ $movie->genre }}</td><td>{{ $movie->release_date?->format('d/m/Y') }}</td><td><span class="badge text-bg-{{ $movie->status==='now_showing'?'success':($movie->status==='upcoming'?'info':'secondary') }}">{{ match($movie->status) {'now_showing' => 'Đang chiếu', 'upcoming' => 'Sắp chiếu', default => 'Ngừng chiếu'} }}</span></td><td class="text-end"><a class="btn btn-outline-dark btn-sm" href="{{ route('admin.movies.edit',$movie) }}"><i class="bi bi-pencil"></i></a><form class="d-inline" method="POST" action="{{ route('admin.movies.destroy',$movie) }}" onsubmit="return confirm('Xóa phim này?')">@csrf @method('DELETE')<button class="btn btn-outline-danger btn-sm"><i class="bi bi-trash"></i></button></form></td></tr>@endforeach</tbody></table></div>{{ $movies->links() }}</div></div>@endsection
